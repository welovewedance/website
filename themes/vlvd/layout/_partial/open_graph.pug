-
 var options = {
        title: page && page.og_title ? page.og_title : 
                    page && page.title ? page.title + " - " + config.title : config.title,
        description: page && page.og_description ? page.og_description : 
                    page && page.description ? page.title : config.description,
        type: "website",
        image: "/imgs/og-image.jpg",
    }

if page && page.og_image
   meta(property="og:image" content= page.og_image)

!=open_graph(options)

<meta property="og:image:height" content="241">
<meta property="og:image:width" content="460">



