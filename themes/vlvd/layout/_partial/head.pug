-
 var title = page && page.title ? page.title + " - " + config.title : config.title;
 var description = page && page.description ? page.title : config.description;

include gtag

title= title
meta(name="description", content=description)

meta(charset="UTF-8")
meta(name="viewport" content="width=device-width, initial-scale=1.0")

include icons
include open_graph

!= css("css/normalize.css")
!= css("https://fonts.googleapis.com/icon?family=Material+Icons")
!= css("https://unpkg.com/material-components-web@latest/dist/material-components-web.min.css")
!= css("css/style.css")

!= js("https://unpkg.com/material-components-web@latest/dist/material-components-web.min.js")